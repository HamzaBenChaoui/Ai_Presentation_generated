# Slide AI frontend - local environment variables
# Copy this file to .env and adjust values as needed.

# API base URL (only used in production builds).
# In dev, Vite proxies /api/* to the backend automatically.
VITE_API_URL=
